<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
    xmlns:context="http://www.springframework.org/schema/context"
    xmlns:tx="http://www.springframework.org/schema/tx" 
    xmlns:mvc="http://www.springframework.org/schema/mvc"
    xsi:schemaLocation="http://www.springframework.org/schema/beans 
       http://www.springframework.org/schema/beans/spring-beans.xsd 
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/tx 
       http://www.springframework.org/schema/tx/spring-tx.xsd
       http://www.springframework.org/schema/mvc 
       http://www.springframework.org/schema/mvc/spring-mvc.xsd" >
    <!-- 配置扫描该包下的注解 -->
    <context:component-scan base-package="com.cyd.controller" /> 
    <!-- 拦截器的配置 -->
    <mvc:interceptors>
    	<mvc:interceptor>
    		<!-- /**表示包括路径及其子路径，即拦截所有controller。如果是/admin/*这种，则会拦截/admin/add,
    		/admin/list等类似请求。而/admin/user/add不被拦截，如果是/admin/**这种，则 /admin/add,
    		/admin/list，/admin/user/add都可以拦截-->
    		<mvc:mapping path="/**"/>
    		<!-- 配置对应的拦截器 -->
    		<bean class="com.cyd.interceptor.LoginInterceptor">
    			<property name="allowedPassUrl">
    				<list>
    					<value>login</value>
    				</list>
    			</property>
    		</bean>
    	</mvc:interceptor>
    </mvc:interceptors>
</beans>